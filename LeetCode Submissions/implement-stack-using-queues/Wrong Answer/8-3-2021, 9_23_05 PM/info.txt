{"id":532715879,"lang":"cpp","time":"3 months, 2 weeks","timestamp":1628005985,"status_display":"Wrong Answer","runtime":"N/A","url":"/submissions/detail/532715879/","is_pending":"Not Pending","title":"Implement Stack using Queues","memory":"N/A","code":"class MyStack {\npublic:\n    /** Initialize your data structure here. */\n    queue<int> q1,q2;\n    int curr_size;\n    MyStack() {\n        curr_size = 0;\n    }\n    \n    /** Push element x onto stack. */\n    void push(int x) {\n        curr_size++;\n        q2.push(x);\n        while(!q1.empty()){\n            q2.push(q1.front());\n            q1.pop();\n        }\n        queue<int> q = q1;\n        q1 = q2;\n        q2 = q;\n    }\n    \n    /** Removes the element on top of the stack and returns that element. */\n    int pop() {\n        return (q1.front());\n        q1.pop();\n        curr_size--;\n    }\n    \n    /** Get the top element. */\n    int top() {\n        return q1.front();\n    }\n    \n    /** Returns whether the stack is empty. */\n    bool empty() {\n        if(q1.empty())\n            return true;\n        else\n            return false;\n    }\n};\n\n/**\n * Your MyStack object will be instantiated and called as such:\n * MyStack* obj = new MyStack();\n * obj->push(x);\n * int param_2 = obj->pop();\n * int param_3 = obj->top();\n * bool param_4 = obj->empty();\n */","compare_result":"1111011011110000","title_slug":"implement-stack-using-queues"}